#include "Start.h"
void result_window(int resX, int resY)
{
    //create a window
    initwindow(resX, resY);
    initfont();
    //setcolor(RED);
  //  setbkcolor(BLACK);
    //mouse and keyboard setup
}